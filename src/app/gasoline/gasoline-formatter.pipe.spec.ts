import { GasolineFormatterPipe } from './gasoline-formatter.pipe';

describe('GasolineFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new GasolineFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
