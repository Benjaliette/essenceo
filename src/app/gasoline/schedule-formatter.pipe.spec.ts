import { ScheduleFormatterPipe } from './schedule-formatter.pipe';

describe('ScheduleFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new ScheduleFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
